<!DOCTYPE html>
<html>
<head>
    <title>Quản lý Lịch trình Tưới</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <h1>Quản lý Lịch trình Tưới</h1>
    <h2>Các lịch trình hiện có</h2>
    <% if (schedules.length > 0) { %>
        <table class="table-common">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Thời gian bắt đầu</th>
                    <th>Thời lượng (phút)</th>
                    <th>Lặp lại</th>
                    <th>Ngày trong tuần</th>
                    <th>Trạng thái</th>
                    <th>Hành động</th>
                </tr>
            </thead>
            <tbody>
                <% schedules.forEach(schedule => { %>
                    <tr>
                        <td><%= schedule.id %></td>
                        <td><%= schedule.start_time %></td>
                        <td><%= schedule.duration %></td>
                        <td><%= schedule.repeat_rule %></td>
                        <td><%= schedule.days_of_week %></td>
                        <td><%= schedule.is_active ? 'Bật' : 'Tắt' %></td>
                        <td>
                            <a href="/sections/<%= sectionId %>/schedules/edit/<%= schedule.id %>">Sửa</a> |
                            <a href="/sections/<%= sectionId %>/schedules/delete/<%= schedule.id %>" onclick="return confirm('Bạn có chắc chắn muốn xóa lịch trình này?')">Xóa</a>
                        </td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
        
    <% } else { %>
        <p>Chưa có lịch trình tưới nào cho vùng này.</p>
    <% } %>
    <a class="btn-back" href="/">Về trang chủ</a>
    <a class="button" href="/sections/<%= sectionId %>/schedules/add">Thêm Lịch trình Mới</a>
</body>
</html>